[toolchain]
anchor_version = "0.31.1"

[provider]
cluster = "Devnet"
wallet = "C:/Users/ssham/.config/solana/id.json"

[registry]
url = "https://anchor.projectserum.com"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"